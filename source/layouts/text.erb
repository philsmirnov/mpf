<!DOCTYPE html>
<html class="no-js" lang="ru" manifest="offline.appcache">
<head>
  <%= partial 'head' %>
</head>
<body>
    <%= partial 'topbar' %>
    <div class="row">
      <div class="large-12 columns">
        <h2 class="app_title"><%= current_page_title %> </h2>
      </div>
    </div>

    <!--div class="row">
      <div class="large-12 columns">
        <p class="app_quote app_gray">И так без конца, без счета, без дней в тихом мраке ночи люди идут мимо Богини, жаждут чего-то, смотрят и дальше идут, и нет конца их движению.<br>
          (Владимир Шмаков. Священная книга Тота)
        </p>
      </div>
    </div-->


    <div class="row">
      <div class="large-12 columns">

        <div class=app_text>
          <%= yield %>
        </div>
      </div>

      <% if current_page.data.read_later %>
          <div class="large-12 columns">
            <h3 class="app_normal app_lgray">Читать дальше:</h3>
            <hr />
          </div>

          <% current_page.data.read_later.each do |text| %>

          <div class="large-4 columns app_smaller">
            <h4 class="app_normal"><a href="<%= text[:link] %>"><%= text[:title] %></a></h4>
            <p>
              <%= link_to truncate(text[:first_paragraph], :length => 150), text[:link], :class => 'app_dgray'  %>
            </p>
          </div>

          <% end %>
      <% end %>
    </div>


    <%= partial 'bottom_scripts' %>
    <%= partial 'analytics' %>
</body>
</html>
